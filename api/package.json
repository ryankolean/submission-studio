{
  "name": "@publication-studio/api",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.json",
    "dev": "wrangler dev",
    "migrate:local": "wrangler d1 migrations apply submission-studio-db --local",
    "migrate:remote": "wrangler d1 migrations apply submission-studio-db --remote",
    "seed-user": "node scripts/seed-user.mjs",
    "test:runtime": "node scripts/runtime-smoke.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  },
  "dependencies": {
    "hono": "^4.13.3"
  }
}
