{
  "name": "submission-studio",
  "private": true,
  "type": "module",
  "workspaces": [
    "api",
    "web"
  ],
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "dev:api": "npm run dev --workspace @submission-studio/api",
    "dev:web": "npm run dev --workspace @submission-studio/web"
  },
  "engines": {
    "node": ">=24"
  }
}
